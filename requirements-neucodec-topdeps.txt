torch==2.6.0
# neucodec==0.0.4 # manually
local-attention>=1.11.1
# torchao # >=0.12.0
torchtune==0.3.0 # >=0.3.1
transformers==4.48.0
vector-quantize-pytorch==1.17.8


# === ADDITIONAL REQUIREMENTS FOR BENCHMARK-AUDIO-TOKENIZER ===
# For running examples/notebooks
datasets==2.21.0  # For loading ESB diagnostic dataset (and for using ffmpeg instead of torchcodec)
matplotlib  # For visualization in notebooks
ipykernel  # For Jupyter kernel
numpy<2.0  # Must be <2.0 for compatibility with torch version of NGC (but xcodec2 needs >=2.0.2)
numba==0.62.1 # to be able to use numpy>=2.0.2

# For running scipts/tokenizer-evaluation.py
static_ffmpeg
stempeg
pesq
pystoi
museval